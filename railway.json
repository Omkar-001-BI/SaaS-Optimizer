{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm start",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300
  },
  "env": {
    "NODE_ENV": "production",
    "ML_SERVICE_URL": "${ML_SERVICE_URL}",
    "MONGODB_URI": "${MONGODB_URI}"
  }
}